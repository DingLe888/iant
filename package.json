{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test:watch": "npx jest --verbose --watch",
    "test": "npx jest --verbose"
  },
  "devDependencies": {
    "@types/jest": "^23.3.12",
    "jest": "^23.6.0",
    "typescript": "^3.2.2",
    "ts-jest": "^23.10.5",
    "lerna": "^3.8.0"
  }
}
